CREATE TABLE UNIT_OF_MEASUREMENTS
(
    ID RAW(16) PRIMARY KEY NOT NULL,
    UOM VARCHAR2(32) NOT NULL,
    DESCRIPTION VARCHAR2(128),
    DELETED NUMBER(1) DEFAULT 0 NOT NULL,
    CREATED TIMESTAMP(6),
    UPDATED TIMESTAMP(6),
    CREATED_BY VARCHAR2(256),
    UPDATED_BY VARCHAR2(256)
);